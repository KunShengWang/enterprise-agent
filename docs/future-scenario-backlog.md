# 采购场景后续工作边界

采购是唯一核心业务场景；General 是受限公共能力。旧业务路线已归档，不作为当前开发待办。

优先完成隔离 PostgreSQL 历史兼容与安全验收、最终工作区提交审计，然后再讨论真实 RFQ 下游适配器及生产认证。真实接口需独立评审幂等、审批、对账与失败策略。

新采购能力、订单创建、付款或新的执行树/聚合预算都需要明确需求和单独授权。本文件列方向，不代表已实现或默认承诺。验收缺口见 [remaining-gaps](remaining-gaps.md)。
