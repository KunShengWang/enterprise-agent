package com.agent.platform.workbench.persistence;

public enum M1ACommitStage {
    INPUT_PERSISTED,
    WORK_ITEM_PERSISTED,
    RELATION_PERSISTED,
    FOCUS_UPDATED,
    BEFORE_EVENT_APPEND,
    EVENT_APPENDED,
    BEFORE_COMMIT
}
