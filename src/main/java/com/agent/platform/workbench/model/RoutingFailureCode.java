package com.agent.platform.workbench.model;

public enum RoutingFailureCode {
    STRUCTURED_OUTPUT_INVALID,
    MODEL_TIMEOUT,
    PROVIDER_ERROR,
    POLICY_REJECTED,
    RESULT_PERSISTENCE_UNKNOWN,
    MODEL_FALLBACK,
    TARGET_DISABLED,
    RETRY_EXHAUSTED
}

